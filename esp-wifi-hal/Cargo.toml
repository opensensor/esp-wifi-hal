[package]
name = "esp-wifi-hal"
version = "0.3.3"
authors = ["Frostie314159 <frostie.neuenhausen@gmail.com>"]
edition = "2024"
license = "MIT OR Apache-2.0"
readme = "README.md"
description = "An experimental driver for the Wi-Fi peripheral on ESP32-series chips."
homepage = "https://github.com/opensensor/esp-wifi-hal"
repository = "https://github.com/opensensor/esp-wifi-hal"

[dependencies]
esp-hal = { version = "1.1.1", features = ["unstable"] }
esp-phy = { version = "=0.2.0", path = "../vendor/esp-phy" }
esp-sync = "0.2.1"

esp32 = { version = "=0.40.2", optional = true, features = [
  "critical-section",
] }
esp32s2 = { version = "=0.31.2", optional = true, features = [
  "critical-section",
] }
esp32c3 = { version = "0.32", optional = true, features = [
  "critical-section",
] }

esp32s3 = { version = "=0.35.2", optional = true, features = ["critical-section"] }
esp-wifi-sys-esp32s3 = { version = "=0.2.0", git = "https://github.com/opensensor/esp-wifi-sys", rev = "73add8985cec3b7582e6df80a4273022deb844b0", optional = true }

esp-wifi-sys-esp32 = { version = "=0.2.0", optional = true }
esp-wifi-sys-esp32s2 = { version = "=0.2.0", optional = true }
esp-wifi-sys-esp32c3 = { version = "=0.2.0", git = "https://github.com/opensensor/esp-wifi-sys", rev = "73add8985cec3b7582e6df80a4273022deb844b0", optional = true }

# Embassy dependencies
embassy-futures = "0.1.2"
embassy-sync = "0.8.0"
embassy-time = "0.5.0"

static_cell = { version = "2.1.1" }
macro-bits = "0.1.5"
portable-atomic = { version = "1.13.1", default-features = false }
defmt-or-log = { version = "0.2.3", default-features = false }
defmt = { version = "1.0.1", optional = true }
bitfield-struct = "0.13.0"
log = { version = "0.4.28", optional = true }
critical-section = "1.2.0"
num = { version = "0.4.3", default-features = false }
esp-wifi-rates = { version = "0.1.0" }

[profile.dev]
# Rust debug is too slow.
# For debug builds always builds with some optimization
opt-level = "s"

[profile.release]
codegen-units = 1        # LLVM can perform better optimizations using a single thread
debug = 2
debug-assertions = false
incremental = false
lto = 'fat'
opt-level = 's'
overflow-checks = false

[features]
esp32 = [
  "dep:esp32",
  "esp-hal/esp32",
  "esp-wifi-sys-esp32",
  "esp-phy/esp32",
  "esp-sync/esp32",
]
esp32s2 = [
  "dep:esp32s2",
  "esp-hal/esp32s2",
  "esp-wifi-sys-esp32s2",
  "esp-phy/esp32s2",
  "esp-sync/esp32s2",
]
esp32c3 = [
  "dep:esp32c3",
  "esp-hal/esp32c3",
  "esp-wifi-sys-esp32c3",
  "esp-phy/esp32c3",
  "esp-sync/esp32c3",
]

esp32s3 = [
  "dep:esp32s3",
  "esp-hal/esp32s3",
  "esp-wifi-sys-esp32s3",
  "esp-phy/esp32s3",
  "esp-sync/esp32s3",
]

defmt = [
  "dep:defmt",
  "defmt-or-log/defmt",
  "esp-phy/defmt",
  "esp-sync/defmt",
  "esp-wifi-rates/defmt",
]
log = ["dep:log", "defmt-or-log/log", "esp-phy/log-04", "esp-sync/log-04"]
critical_section = []
# Optional in-memory latency counters; no output in interrupt/packet paths.
timing-probe = []

[patch.crates-io]
# S3 Wi-Fi registers pending an upstream PAC release (0.35 compatibility branch).
esp32s3 = { git = "https://github.com/opensensor/esp-pacs", rev = "37b54bd9ad62de17b17a8d7984a2ace5e73d63dd" }
# esp32c3 PAC with the Wi-Fi MAC register block (esp-rs/esp-pacs#510), on top of the 0.32.3 release.
esp32c3 = { git = "https://github.com/opensensor/esp-pacs", rev = "9be190016260ef195c97f7aa5aa7c576a1646890" }
